<Page x:Class="MDTManagment.Views.DentistPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MDTManagment.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="DentistPage">

    <ListView ItemsSource="{Binding AdditionalDentist}" IsSynchronizedWithCurrentItem="True">
        <ListView.View>
            <GridView>
                <GridView.Columns>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="MiddleName" DisplayMemberBinding="{Binding MiddleName}"/>
                    <GridViewColumn Header="LastName" DisplayMemberBinding="{Binding LastName}"/>
                    <GridViewColumn Header="Contact" DisplayMemberBinding="{Binding Contact}"/>
                    <GridViewColumn Header="ProfessionalExperience" DisplayMemberBinding="{Binding ProfessionalExperience}"/>
                </GridView.Columns>
            </GridView>
        </ListView.View>
    </ListView>
</Page>
