<Page x:Class="MDTManagment.Views.Patients.PatientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="PatientPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Patient.FirstName}"/>
        <TextBlock Grid.Column="1" Text="{Binding Patient.Surname}"/>
        <TextBlock Grid.Column="2" Text="{Binding Patient.Family}"/>
        <TextBlock Grid.Column="3" Text="{Binding Patient.Age}"/>
        <TextBlock Grid.Column="4" Text="{Binding Patient.Address}"/>
        <TextBlock Grid.Column="5" Text="{Binding Patient.PhoneNumber}"/>
    </Grid>

</Page>
