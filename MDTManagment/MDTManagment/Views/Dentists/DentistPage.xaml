<Page x:Class="MDTManagment.Views.Dentists.DentistPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MDTManagment.Views.Dentists"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="DentistPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Dentist.Name}"/>
        <TextBlock Grid.Column="1"   Text="{Binding Dentist.MiddleName}"/>
        <TextBlock Grid.Column="2"   Text="{Binding Dentist.LastName}"/>
        <TextBlock Grid.Column="3"   Text="{Binding Dentist.Contact}"/>
        <TextBlock Grid.Column="4"   Text="{Binding Dentist.ProfessionalExperience}"/>
    </Grid>
</Page>
